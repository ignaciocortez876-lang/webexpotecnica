// scripts.js
document.addEventListener("DOMContentLoaded", (event) => {
  console.log("Página de presentación del Invernadero cargada correctamente.");

  // Aquí puedes agregar funciones de interactividad si las necesitas más adelante,
  // como: animaciones al hacer scroll o un simulador de datos en tiempo real.

  // Ejemplo de mensaje de bienvenida simple:
  const header = document.querySelector("header h1");
  if (header) {
    // Podrías cambiar el color o añadir una clase al cargar.
    // header.style.color = 'var(--color-acento)';
  }
});
